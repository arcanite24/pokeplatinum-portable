#include "versions.h"

// Global game language setting (default to English for SDL build)  
int gGameLanguage = 1; // English

// Global cartridge wake flag (false for SDL build)
int gIgnoreCartridgeForWake = 0;